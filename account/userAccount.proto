syntax = "proto3";

option go_package = "api-login-proto/login";

import "api-login-proto/common/request.proto";
import "api-login-proto/common/response.proto";

service UserAccountService {
  rpc Info(common.Request) returns (common.Response);
  rpc List(common.Request) returns (common.Response);
}
